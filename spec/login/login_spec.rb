require 'spec_helper'

describe LoginPage do

  it 'login page'

  # let (:login_page) { LoginPage.new(@driver) }
  # let (:valid_pass) { ADMIN1[:password] }
  # let (:valid_user) { ADMIN1[:login] }

  # before(:all) do
  #   LoginPage.new(@driver).goto
  # end

  # context 'using valid cred' do
  #
  #   it 'downcase username', :smoke do
  #     login_page = LoginPage.new(@driver)
  #     login_page.login_with('rkukharuk@gmail.com', 'getbase')
  #     # home_page = login_page.login_with valid_user.downcase, valid_pass
  #     # expect(home_page).to be_loaded
  #     # home_page.header.logout
  #   end
  # end
end